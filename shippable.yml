build:
  ci:
    - docker build -t repo.treescale.com/ultravisual/ultravisual-site:0.0.1 .
    - docker push repo.treescale.com/ultravisual/ultravisual-site:0.0.1
integrations:
  hub:
    - integrationName: treescale
      type: dockerLogin